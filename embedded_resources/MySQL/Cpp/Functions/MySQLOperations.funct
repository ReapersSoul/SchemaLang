{% if header %}
static bool MySQLSetConnection(std::shared_ptr<mysqlx::Session> session);
static std::shared_ptr<mysqlx::Session> MySQLGetConnection();
static bool MySQLCreateTable();
bool MySQLInsert();
bool MySQLUpdate();
bool MySQLDelete();
const mysqlx::abi2::r0::Row toMySQLRow() const;
static std::shared_ptr<{{identifier}}Schema> fromMySQLRow(const mysqlx::abi2::r0::Row& row);
static std::vector<std::shared_ptr<{{identifier}}Schema>> MySQLSelectAll();
{% for mv in member_variables %}
{% if not (mv.type.is_array or mv.type.is_struct or mv.type.is_enum) %}
static std::vector<std::shared_ptr<{{identifier}}Schema>> MySQLSelectBy{{mv.identifierCamel}}({{mv.type.estimated}} {{mv.identifier}});
{% endif %}
{% endfor %}
//getter and setter for mysqlx::Session
static void SetMySQLSession(std::shared_ptr<mysqlx::Session> session) { mysql_session = session; }
static std::shared_ptr<mysqlx::Session> GetMySQLSession() { return mysql_session; }
//getter and setter for database name
static void SetDatabaseName(const std::string& db_name) { database_name = db_name; }
static std::string GetDatabaseName() { return database_name; }
{% else %}
bool {{identifier}}Schema::MySQLSetConnection(std::shared_ptr<mysqlx::Session> session)
{
    if(session == nullptr) {
        std::cerr << "MySQL session pointer is null!" << std::endl;
        return false;
    }
    {{identifier}}Schema::mysql_session = session;
    if(!MySQLCreateTable()) {
        std::cerr << "Failed to create table for {{identifier}} in MySQL database." << std::endl;
        return false;
    }
    std::cout << "MySQL connection set for {{identifier}} schema." << std::endl;
    return true;
}

std::shared_ptr<mysqlx::Session> {{identifier}}Schema::MySQLGetConnection()
{
    return {{identifier}}Schema::mysql_session;
}

bool {{identifier}}Schema::MySQLCreateTable()
{
    if(mysql_session == nullptr) {
        std::cerr << "MySQL session not set! Call MySQLSetConnection first." << std::endl;
        return false;
    }
    
    try {
        auto schema = mysql_session->getSchema(database_name);
        std::string create_sql = "CREATE TABLE IF NOT EXISTS {{identifier}} ("
{% for mv in member_variables %}
{% if not (mv.type.is_array or mv.type.is_struct or mv.type.is_enum) %}
                "{{mv.identifier}} {% if mv.type.is_integer %}INT{% endif %}{% if mv.type.is_real %}DOUBLE{% endif %}{% if mv.type.is_string %}VARCHAR(255){% endif %}{% if mv.type.is_bool %}BOOLEAN{% endif %}{% if not (mv.type.is_integer or mv.type.is_real or mv.type.is_string or mv.type.is_bool) %}TEXT{% endif %}{% if mv.primary_key %} PRIMARY KEY{% endif %}{% if mv.auto_increment %} AUTO_INCREMENT{% endif %}{% if mv.required %} NOT NULL{% endif %}{% if not loop.is_last %}, {% endif %}"
{% endif %}
{% endfor %}
                            ")";
        
        mysql_session->sql(create_sql).execute();
        return true;
    }
    catch (const mysqlx::Error& e) {
        std::cerr << "MySQL error: " << e.what() << std::endl;
        return false;
    }
}

bool {{identifier}}Schema::MySQLInsert()
{
    if(mysql_session == nullptr) {
        std::cerr << "MySQL session not set! Call MySQLSetConnection first." << std::endl;
        return false;
    }
    
    try {
        auto schema = mysql_session->getSchema(database_name);
        auto table = schema.getTable("{{identifier}}");
        
    auto insert = table.insert({% for mv in member_variables %}{% if not (mv.type.is_array or mv.type.is_struct or mv.type.is_enum) %}"{{mv.identifier}}"{% if not loop.is_last %}, {% endif %}{% endif %}{% endfor %});
    insert.values({% for mv in member_variables %}{% if not (mv.type.is_array or mv.type.is_struct or mv.type.is_enum) %}this->{{mv.identifier}}{% if not mv.required %}.value_or({{mv.type.estimated}}{}){% endif %}{% if not loop.is_last %}, {% endif %}{% endif %}{% endfor %});
        insert.execute();
        return true;
    }
    catch (const mysqlx::Error& e) {
        std::cerr << "MySQL insert error: " << e.what() << std::endl;
        return false;
    }
}

bool {{identifier}}Schema::MySQLUpdate()
{
    if(mysql_session == nullptr) {
        std::cerr << "MySQL session not set! Call MySQLSetConnection first." << std::endl;
        return false;
    }
    
    try {
        auto schema = mysql_session->getSchema(database_name);
        auto table = schema.getTable("{{identifier}}");
        
        auto update = table.update();
{% for mv in member_variables %}
{% if not mv.primary_key and not (mv.type.is_array or mv.type.is_struct or mv.type.is_enum) %}
    update.set("{{mv.identifier}}", this->{{mv.identifier}}{% if not mv.required %}.value_or({{mv.type.estimated}}{}){% endif %});
{% endif %}
{% endfor %}
        
        // Add WHERE clause for primary key
{% for mv in member_variables %}
{% if mv.primary_key and not (mv.type.is_array or mv.type.is_struct or mv.type.is_enum) %}
    update.where("{{mv.identifier}} = :pk").bind("pk", this->{{mv.identifier}}{% if not mv.required %}.value_or({{mv.type.estimated}}{}){% endif %});
{% endif %}
{% endfor %}
        
        update.execute();
        return true;
    }
    catch (const mysqlx::Error& e) {
        std::cerr << "MySQL update error: " << e.what() << std::endl;
        return false;
    }
}

bool {{identifier}}Schema::MySQLDelete()
{
    if(mysql_session == nullptr) {
        std::cerr << "MySQL session not set! Call MySQLSetConnection first." << std::endl;
        return false;
    }
    
    try {
        auto schema = mysql_session->getSchema(database_name);
        auto table = schema.getTable("{{identifier}}");
        
        auto remove = table.remove();
        
        // Add WHERE clause for primary key
{% for mv in member_variables %}
{% if mv.primary_key and not (mv.type.is_array or mv.type.is_struct or mv.type.is_enum) %}
    remove.where("{{mv.identifier}} = :pk").bind("pk", this->{{mv.identifier}}{% if not mv.required %}.value_or({{mv.type.estimated}}{}){% endif %});
{% endif %}
{% endfor %}
        
        remove.execute();
        return true;
    }
    catch (const mysqlx::Error& e) {
        std::cerr << "MySQL delete error: " << e.what() << std::endl;
        return false;
    }
}

std::vector<std::shared_ptr<{{identifier}}Schema>> {{identifier}}Schema::MySQLSelectAll()
{
    std::vector<std::shared_ptr<{{identifier}}Schema>> results;
    
    if(mysql_session == nullptr) {
        std::cerr << "MySQL session not set! Call MySQLSetConnection first." << std::endl;
        return results;
    }
    
    try {
        auto schema = mysql_session->getSchema("{{identifier}}");
        auto table = schema.getTable("{{identifier}}");
        
        auto select = table.select({% for mv in member_variables %}{% if not (mv.type.is_array or mv.type.is_struct or mv.type.is_enum) %}"{{mv.identifier}}"{% if not loop.is_last %}, {% endif %}{% endif %}{% endfor %});
        auto result = select.execute();
        
        while (auto row = result.fetchOne()) {
            std::shared_ptr<{{identifier}}Schema> obj = std::make_shared<{{identifier}}Schema>();
{% set column_index = 0 %}
{% for mv in member_variables %}
{% if not (mv.type.is_array or mv.type.is_struct or mv.type.is_enum) %}
            // Set {{mv.identifier}}
{% if mv.type.is_integer %}
            obj->set{{mv.identifierCamel}}(row[{{column_index}}].get<int>());
{% endif %}
{% if mv.type.is_real %}
            obj->set{{mv.identifierCamel}}(row[{{column_index}}].get<double>());
{% endif %}
{% if mv.type.is_string %}
            obj->set{{mv.identifierCamel}}(row[{{column_index}}].get<std::string>());
{% endif %}
{% if mv.type.is_bool %}
            obj->set{{mv.identifierCamel}}(row[{{column_index}}].get<bool>());
{% endif %}
{% if not (mv.type.is_integer or mv.type.is_real or mv.type.is_string or mv.type.is_bool) %}
            obj->set{{mv.identifierCamel}}(row[{{column_index}}].get<{{mv.type.estimated}}>());
{% endif %}
{% set column_index = column_index + 1 %}
{% endif %}
{% endfor %}
            results.push_back(obj);
        }
        
        return results;
    }
    catch (const mysqlx::Error& e) {
        std::cerr << "MySQL select error: " << e.what() << std::endl;
        return results;
    }
}

{% for mv in member_variables %}
{% if not (mv.type.is_array or mv.type.is_struct or mv.type.is_enum) %}
std::vector<std::shared_ptr<{{identifier}}Schema>> {{identifier}}Schema::MySQLSelectBy{{mv.identifierCamel}}({{mv.type.estimated}} {{mv.identifier}})
{
    std::vector<std::shared_ptr<{{identifier}}Schema>> results;
    
    if(mysql_session == nullptr) {
        std::cerr << "MySQL session not set! Call MySQLSetConnection first." << std::endl;
        return results;
    }
    
    try {
        auto schema = mysql_session->getSchema("{{identifier}}");
        auto table = schema.getTable("{{identifier}}");
        
        auto select = table.select({% for mv2 in member_variables %}{% if not (mv2.type.is_array or mv2.type.is_struct or mv2.type.is_enum) %}"{{mv2.identifier}}"{% if not loop.is_last %}, {% endif %}{% endif %}{% endfor %})
                          .where("{{mv.identifier}} = :value")
                          .bind("value", {{mv.identifier}});
        auto result = select.execute();
        
        while (auto row = result.fetchOne()) {
            std::shared_ptr<{{identifier}}Schema> obj = std::make_shared<{{identifier}}Schema>();
{% set column_index = 0 %}
{% for mv2 in member_variables %}
{% if not (mv2.type.is_array or mv2.type.is_struct or mv2.type.is_enum) %}
            // Set {{mv2.identifier}}
{% if mv2.type.is_integer %}
            obj->set{{mv2.identifierCamel}}(row[{{column_index}}].get<int>());
{% endif %}
{% if mv2.type.is_real %}
            obj->set{{mv2.identifierCamel}}(row[{{column_index}}].get<double>());
{% endif %}
{% if mv2.type.is_string %}
            obj->set{{mv2.identifierCamel}}(row[{{column_index}}].get<std::string>());
{% endif %}
{% if mv2.type.is_bool %}
            obj->set{{mv2.identifierCamel}}(row[{{column_index}}].get<bool>());
{% endif %}
{% if not (mv2.type.is_integer or mv2.type.is_real or mv2.type.is_string or mv2.type.is_bool) %}
            obj->set{{mv2.identifierCamel}}(row[{{column_index}}].get<{{mv2.type.estimated}}>());
{% endif %}
{% set column_index = column_index + 1 %}
{% endif %}
{% endfor %}
            results.push_back(obj);
        }
        
        return results;
    }
    catch (const mysqlx::Error& e) {
        std::cerr << "MySQL select error: " << e.what() << std::endl;
        return results;
    }
}

{% endif %}
{% endfor %}

const mysqlx::abi2::r0::Row {{identifier}}Schema::toMySQLRow() const
{
    mysqlx::abi2::r0::Row row;
{% set column_index = 0 %}
{% for mv in member_variables %}
{% if not (mv.type.is_array or mv.type.is_struct or mv.type.is_enum) %}
    row.set({{column_index}}, this->{{mv.identifier}}{% if not mv.required %}.value_or({{mv.type.estimated}}{}){% endif %});
{% set column_index = column_index + 1 %}
{% endif %}
{% endfor %}
    return row;
}

{% endif %}
