{% if header %}
void SQLiteSelect(SQLiteDB* db);
{% else %}
void {{identifier}}Schema::SQLiteSelect(SQLiteDB* db) {
    const char* sql = R"(
        SELECT 
    )";

    sqlite3_stmt* stmt;
    if (sqlite3_prepare_v2(db->getDB(), sql, -1, &stmt, nullptr) != SQLITE_OK) {
        throw std::runtime_error("{{identifier}}Schema::SQLiteSelect() - Failed to prepare SELECT statement: " + std::string(sqlite3_errmsg(db->getDB())) + "\\nSQL: " + std::string(sql));
    }

    while (sqlite3_step(stmt) == SQLITE_ROW) {

    }

    sqlite3_finalize(stmt);
}
{% endif %}
