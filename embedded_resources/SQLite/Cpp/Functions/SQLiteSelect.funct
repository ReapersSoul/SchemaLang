{% if header %}
static std::vector<shared_ptr<{{identifier}}Schema>> SQLiteSelectAll(sqlite3* db);
static std::vector<shared_ptr<{{identifier}}Schema>> SQLiteSelectAll();
{% for mv in member_variables %}
static std::vector<shared_ptr<{{identifier}}Schema>> SQLiteSelectBy{{mv.identifierCamel}}(sqlite3* db, {{mv.type}} {{mv.identifier}});
static std::vector<shared_ptr<{{identifier}}Schema>> SQLiteSelectBy{{mv.identifierCamel}}({{mv.type}} {{mv.identifier}});
{% endfor %}
{% else %}
std::vector<shared_ptr<{{identifier}}Schema>> {{identifier}}Schema::SQLiteSelectAll(sqlite3* db)
{
    std::vector<shared_ptr<{{identifier}}Schema>> results;
    std::string sql = "SELECT {% for mv in member_variables %}{{mv.identifier}}{% if not loop.is_last %}, {% endif %}{% endfor %} FROM {{identifier}}";
    
    sqlite3_stmt *stmt;
    int rc = sqlite3_prepare_v2(db, sql.c_str(), -1, &stmt, NULL);
    if(rc != SQLITE_OK) {
        std::cerr << "SQLite prepare error: " << sqlite3_errmsg(db) << std::endl;
        return results;
    }
    
    while(sqlite3_step(stmt) == SQLITE_ROW) {
        shared_ptr<{{identifier}}Schema> result = new {{identifier}}Schema();
{% for mv in member_variables %}
        // Column {{loop.index}}: {{mv.identifier}}
    {% if mv.type.is_integer %}
        result->set{{mv.identifierCamel}}(sqlite3_column_int(stmt, {{loop.index}}));
    {% endif %}
    {% if mv.type.is_real %}
        result->set{{mv.identifierCamel}}(sqlite3_column_double(stmt, {{loop.index}}));
    {% endif %}
    {% if mv.type.is_string %}
        const char* {{mv.identifier}}_str = reinterpret_cast<const char*>(sqlite3_column_text(stmt, {{loop.index}}));
        if({{mv.identifier}}_str) result->set{{mv.identifierCamel}}(std::string({{mv.identifier}}_str));
    {% endif %}
    {% if mv.type.is_bool %}
        result->set{{mv.identifierCamel}}(sqlite3_column_int(stmt, {{loop.index}}) != 0);
    {% endif %}
    {% if not (mv.type.is_integer or mv.type.is_real or mv.type.is_string or mv.type.is_bool) %}
        const char* {{mv.identifier}}_str = reinterpret_cast<const char*>(sqlite3_column_text(stmt, {{loop.index}}));
        if({{mv.identifier}}_str) {
            // Handle custom type conversion here
            // result->set{{mv.identifierCamel}}(convert_from_string({{mv.identifier}}_str));
        }
    {% endif %}
{% endfor %}
        results.push_back(result);
    }
    
    sqlite3_finalize(stmt);
    return results;
}

std::vector<shared_ptr<{{identifier}}Schema>> {{identifier}}Schema::SQLiteSelectAll()
{
    if(db == nullptr) {
        std::cerr << "Database not set! Call SQLiteSetDatabase first." << std::endl;
        return std::vector<shared_ptr<{{identifier}}Schema>>();
    }
    return SQLiteSelectAll(db);
}

{% for mv in member_variables %}
std::vector<shared_ptr<{{identifier}}Schema>> {{identifier}}Schema::SQLiteSelectBy{{mv.identifierCamel}}(sqlite3* db, {{mv.type}} {{mv.identifier}})
{
    std::vector<shared_ptr<{{identifier}}Schema>> results;
    std::string sql = "SELECT {% for mv2 in member_variables %}{{mv2.identifier}}{% if not loop.is_last %}, {% endif %}{% endfor %} FROM {{identifier}} WHERE {{mv.identifier}} = ?";
    
    sqlite3_stmt *stmt;
    int rc = sqlite3_prepare_v2(db, sql.c_str(), -1, &stmt, NULL);
    if(rc != SQLITE_OK) {
        std::cerr << "SQLite prepare error: " << sqlite3_errmsg(db) << std::endl;
        return results;
    }
    
    // Bind the search parameter
    {% if mv.type.is_integer %}
    sqlite3_bind_int(stmt, 1, {{mv.identifier}});
    {% endif %}
    {% if mv.type.is_real %}
    sqlite3_bind_double(stmt, 1, {{mv.identifier}});
    {% endif %}
    {% if mv.type.is_string %}
    sqlite3_bind_text(stmt, 1, {{mv.identifier}}.c_str(), -1, SQLITE_STATIC);
    {% endif %}
    {% if mv.type.is_bool %}
    sqlite3_bind_int(stmt, 1, {{mv.identifier}} ? 1 : 0);
    {% endif %}
    {% if not (mv.type.is_integer or mv.type.is_real or mv.type.is_string or mv.type.is_bool) %}
    sqlite3_bind_text(stmt, 1, std::to_string({{mv.identifier}}).c_str(), -1, SQLITE_STATIC);
    {% endif %}
    
    while(sqlite3_step(stmt) == SQLITE_ROW) {
        shared_ptr<{{identifier}}Schema> result = new {{identifier}}Schema();
{% for mv2 in member_variables %}
        // Column {{loop.index}}: {{mv2.identifier}}
    {% if mv2.type.is_integer %}
        result->set{{mv2.identifier}}(sqlite3_column_int(stmt, {{loop.index}}));
    {% endif %}
    {% if mv2.type.is_real %}
        result->set{{mv2.identifier}}(sqlite3_column_double(stmt, {{loop.index}}));
    {% endif %}
    {% if mv2.type.is_string %}
        const char* {{mv2.identifier}}_str = reinterpret_cast<const char*>(sqlite3_column_text(stmt, {{loop.index}}));
        if({{mv2.identifier}}_str) result->set{{mv2.identifier}}(std::string({{mv2.identifier}}_str));
    {% endif %}
    {% if mv2.type.is_bool %}
        result->set{{mv2.identifier}}(sqlite3_column_int(stmt, {{loop.index}}) != 0);
    {% endif %}
    {% if not (mv2.type.is_integer or mv2.type.is_real or mv2.type.is_string or mv2.type.is_bool) %}
        const char* {{mv2.identifier}}_str = reinterpret_cast<const char*>(sqlite3_column_text(stmt, {{loop.index}}));
        if({{mv2.identifier}}_str) {
            // Handle custom type conversion here
            // result->set{{mv2.identifier}}(convert_from_string({{mv2.identifier}}_str));
        }
    {% endif %}
{% endfor %}
        results.push_back(result);
    }
    
    sqlite3_finalize(stmt);
    return results;
}

std::vector<shared_ptr<{{identifier}}Schema>> {{identifier}}Schema::SQLiteSelectBy{{mv.identifierCamel}}({{mv.type}} {{mv.identifier}})
{
    if(db == nullptr) {
        std::cerr << "Database not set! Call SQLiteSetDatabase first." << std::endl;
        return std::vector<shared_ptr<{{identifier}}Schema>>();
    }
    return SQLiteSelectBy{{mv.identifier}}(db, {{mv.identifier}});
}

{% endfor %}
{% endif %}
