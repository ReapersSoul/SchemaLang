{% if header %}
bool SQLiteDelete(std::shared_ptr<SQLiteDB> db);
{% else %}
bool {{identifier}}Schema::SQLiteDelete(std::shared_ptr<SQLiteDB> db)
{
    return db->delete{{identifierCamel}}ById(this->getId());
}
{% endif %}